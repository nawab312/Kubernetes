apiVersion: v1 
kind: ConfigMap
metadata:
  name: app-config
data:
  FRONTEND_URL: "http://frontend-service"
  BACKEND_URL: "http://backend-service"
  DB_HOST: "database-service"
  DB_PORT: 3306
  DB_NAME: "appdb"
